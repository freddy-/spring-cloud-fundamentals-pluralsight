spring:
  application:
    name: config-client-app

  cloud:
    config:
      discovery:
        enabled: true

eureka:
  client:
    server-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: refresh